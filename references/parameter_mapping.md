# Parameter Mapping

## Mapping Table

| Parameter ID | Config Path | Implemented Value | Source Paper ID | Evidence (quote/figure/table) | Decision Type | Notes |
|---|---|---|---|---|---|---|
| `phase_order` | `task.phase_order` | `instructions -> learning -> transfer -> goodbye` | `W2125735154` | The standard implementation starts with instructions, runs learning blocks until criterion, then proceeds to the transfer test. | explicit | Framework-level ordering only. |
| `learning_conditions` | `task.learning_conditions` | `[train_ab, train_cd, train_ef]` | `W2014979870` | Figure 1 and methods describe the three acquisition pairs AB, CD, and EF. | explicit | These are the only rewarded learning pairs. |
| `transfer_conditions` | `task.transfer_conditions` | `[test_ab, test_ac, test_ad, test_ae, test_af, test_bc, test_bd, test_be, test_bf, test_cd, test_ce, test_cf, test_de, test_df, test_ef]` | `W2125735154` | The standard transfer phase presents all possible symbol combinations. | explicit | 15 unique pairings. |
| `learning_trials_per_block` | `task.learning.trials_per_block` | `60` | `W2125735154` | The task manual states that each learning block contains 60 trials, 20 per symbol pair. | explicit | Training block size. |
| `learning_trials_per_pair` | `task.learning.trials_per_pair` | `20` | `W2125735154` | The manual specifies 20 trials for each of the three acquisition pairs within a learning block. | explicit | Balanced pair counts within each block. |
| `learning_probability_map` | `task.learning.probabilities` | `AB=0.80/0.20; CD=0.70/0.30; EF=0.60/0.40` | `W2014979870` | The classic PSS learning contingencies are AB 80/20, CD 70/30, and EF 60/40. | explicit | Higher-probability role is the correct choice on learning trials. |
| `learning_criterion_map` | `task.learning.criteria` | `AB>=0.65; CD>=0.60; EF>=0.50` | `W2125735154` | The standard implementation repeats learning blocks until the 65/60/50 criteria are met. | explicit | Criterion check occurs at the end of each learning block. |
| `max_learning_blocks` | `task.learning.max_blocks` | `6` | `inferred` | A finite safety cap is needed even though the literature says to repeat learning blocks until criterion is met. | inferred | Prevents infinite loops in QA/simulation. |
| `transfer_trials_total` | `task.transfer.trials_total` | `150` | `W2125735154` | The standard implementation defaults to 150 transfer trials. | explicit | 10 repetitions of each pair. |
| `transfer_trials_per_pair` | `task.transfer.trials_per_pair` | `10` | `W2125735154` | The manual reports 150 transfer trials for 15 pair combinations, which implies 10 repeats per pair. | explicit | Derived from the manual default. |
| `role_assignment_policy` | `task.roles.randomize_pool` | `shuffle six Hiragana symbols into A-F roles per participant` | `W2125735154` | The manual states that each of the six symbols is randomly assigned to play the role of letters A-F. | explicit | The actual glyph pool is centralized in config. |
| `left_right_balance_policy` | `task.left_right_balance` | `50/50 within each block` | `W2125735154` | The manual states that A appears on the left side of the screen in 50% of trials, and the same is true for the other symbols. | explicit | Left/right positions must be balanced exactly within each block. |
| `response_key_map` | `task.response_keys.left` / `task.response_keys.right` | `left = A, right = L` | `W2125735154` | The manual uses `A` for left and `L` for right responses. | explicit | Keep the key labels config-driven. |
| `ready_duration_ms` | `timing.ready_ms` | `3000` | `W2125735154` | The manual gives a 3000 ms ready duration before the block begins. | explicit | Block-start transition screen. |
| `response_timeout_ms` | `timing.response_timeout_ms` | `4000` | `W2170143116` | The open-access Parkinson's disease variant uses a 4 s stimulus window in the same PSS family. | inferred | Needed for bounded runtime and QA; the base manual excerpt does not expose a universal timeout. |
| `feedback_duration_ms` | `timing.feedback_ms` | `1000` | `W2125735154` | The manual gives a 1000 ms feedback duration. | explicit | Learning feedback only. |
| `iti_duration_ms` | `timing.iti_ms` | `1000` | `inferred` | A short blank/fixation interval keeps the block duration close to the manual's approximate trial timing. | inferred | Used after both learning and transfer trials. |
| `symbol_pool` | `stimuli.symbol_pool` | `six fixed Hiragana characters randomized to roles A-F` | `W2014979870` | The figure and methods refer to six Japanese Hiragana characters, but not the literal glyph names. | inferred | The exact glyph subset is centralized in config. |
| `symbol_font` | `stimuli.symbol_font` | `MS Gothic` | `inferred` | Kana-capable rendering is required for the symbol pool. | inferred | Implementation choice, not protocol text. |
| `instruction_font` | `stimuli.instruction_font` | `SimHei` | `inferred` | Chinese participant text is localized in config. | inferred | Matches the repo's Chinese-language task convention. |
| `symbol_layout` | `stimuli.symbol_layout` | `left x ≈ -220 px, right x ≈ +220 px, height ≈ 180-200 px` | `W2125735154` | The manual places the symbols at left and right positions and uses a large symbol display. | inferred | Exact pixel values are tuned for a 1280 x 720 window. |
| `score_delta_map` | `task.score` | `correct = +1; incorrect = 0` | `inferred` | The learning phase gives binary correct/incorrect feedback, but the literature does not define a monetary score. | inferred | Hidden cumulative count only; no penalty is shown to participants. |
| `test_feedback_enabled` | `task.transfer.feedback_enabled` | `false` | `W2014979870` | The transfer phase is presented without feedback to measure learned preference. | explicit | No learning signal during transfer. |
| `no_response_policy` | `task.no_response_policy` | `timeout counts as incorrect on learning; omission on transfer` | `inferred` | The literature emphasizes choice proportions and feedback learning, but not a universal timeout policy. | inferred | Keep logs for both learning and transfer. |
